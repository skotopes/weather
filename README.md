# About

Weather station for Asagiri paragliding school.
Works with Ecowitt wn90lp sensor and external video camera.

# Running

Create `config_local.py`, override variables that you want to set.

Run `poetry run ./app.py`