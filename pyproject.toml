[project]
name = "weather"
version = "0.1.0"
license = {text = "GPL"}
readme = "README.md"
requires-python = ">=3.9,<4.0"
dependencies = [
    "nicegui (>=2.24.1,<3.0.0)",
    "ecowitt-wn90lp (>=1.2.0,<2.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
